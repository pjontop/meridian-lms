<% content_for :title, "New announcement" %>

<div class="max-w-2xl mx-auto px-4 py-8">
  <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
    <h1 class="font-bold text-4xl mb-6">New Announcement</h1>

    <%= render "form", announcement: @announcement %>

    <%= link_to "Back to classroom", classroom_path(@classroom), class: "mt-4 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-200 text-gray-800 inline-block font-medium" %>
  </div>
</div>
